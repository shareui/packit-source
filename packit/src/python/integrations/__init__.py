# pyright: reportMissingImports=false
# SPDX-License-Identifier: GPL-3.0-or-later

# Everything that reaches into a screen the client owns — the chat, the chat
# list, the profile, the client's own settings — rather than a screen of ours.
